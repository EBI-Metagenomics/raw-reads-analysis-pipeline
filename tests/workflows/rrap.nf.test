nextflow_workflow {

    name "Test Workflow PIPELINE"
    script "workflows/rrap.nf"
    workflow "PIPELINE"

    test("Should run without failures") {

        when {
            params {
	            samplesheet = "${projectDir}/tests/fixtures/rrap/samplesheet_basic.csv"
		        outdir = "$outputDir"
            }
        }

        then {
            def sample_outdir = "${params.outdir}/test_sample"
            def stable_path = getAllFilesFromDir(
                sample_outdir, relative: true, includeDir: true, 
                ignore: ['pipeline_info/*.{html,json,txt}',]
            )
            // stable_md5: All files in ${params.outdir}/test_sample/ with stable content
            def stable_md5 = getAllFilesFromDir(sample_outdir, ignoreFile: 'tests/.nftignore')

            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    stable_path,
                    stable_md5,
                ).match() }
            ) 
        }

        cleanup {
            new File("${launchDir}/download_cache").deleteDir()
            new File("${workDir}").deleteDir()
        }
    }
}
